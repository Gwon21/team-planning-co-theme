<div class="not-found-page">
  <div class="not-found-page__inner">
    <span class="not-found-page__code">404</span>
    <h1 class="not-found-page__title">{{ '404.title' | t }}</h1>
    <p class="not-found-page__message">{{ '404.message' | t }}</p>
    <a class="btn btn--primary" href="{{ routes.root_url }}">{{ '404.back_home' | t }}</a>
  </div>
</div>

<style>
  .not-found-page {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-surface);
    padding: 8rem 2rem;
    text-align: center;
  }
  .not-found-page__code {
    display: block;
    font-family: 'Epilogue', sans-serif;
    font-weight: 900;
    font-size: 10rem;
    color: rgba(195, 198, 206, 0.3);
    line-height: 1;
    margin-bottom: 0;
  }
  .not-found-page__title {
    font-family: 'Epilogue', sans-serif;
    font-weight: 900;
    font-size: clamp(2rem, 4vw, 3.5rem);
    text-transform: uppercase;
    color: var(--color-primary);
    margin: 0 0 1rem;
  }
  .not-found-page__message {
    font-family: 'Manrope', sans-serif;
    font-size: 1rem;
    color: var(--color-on-surface-variant);
    margin: 0 0 2.5rem;
  }
</style>
